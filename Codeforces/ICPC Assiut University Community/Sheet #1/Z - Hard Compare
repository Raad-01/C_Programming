#include <stdio.h>
#include <math.h>

int main()
{
    long long int a, b, c, d;
    long double val1, val2;
    scanf("%lld %lld %lld %lld", &a, &b, &c, &d);

    val1=b*log10(a);
    val2=d*log10(c);

    if(val1>val2)
        printf("YES");
    else
        printf("NO");

    return 0;
}
