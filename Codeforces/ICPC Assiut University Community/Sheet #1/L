#include <stdio.h>
#include <string.h>

int main()
{
    char x[1000], y[1000], z[1000], xyz[1000];
    scanf("%s%s", x, y);
    scanf("%s%s", z, xyz);
    if( strcmp(y, xyz) == 0 )
        printf("ARE Brothers\n");
    else
        printf("NOT\n");
    return 0;
}
