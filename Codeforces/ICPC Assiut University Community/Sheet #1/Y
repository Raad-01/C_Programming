#include <stdio.h>
int main()
{
    int a, b, c, d, ans;
    scanf("%d%d%d%d", &a, &b, &c, &d);
    ans = ((a%100)*(b%100)*(c%100)*(d%100))%100;
    if (ans<=9)
        printf("0%d\n", ans);
    else
         printf("%d\n", ans);

    return 0;
}
