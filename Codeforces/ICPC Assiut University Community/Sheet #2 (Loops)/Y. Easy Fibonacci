#include <stdio.h>

int main()
{
	int n, i, x=0, y=1, sum=0;
	scanf("%d", &n);
	
	if(n==1)
		printf("0");
	
	else if(n==2)
		printf("0 1");
		
	else
	{
		printf("0");
		printf(" 1");
		
		for(i=3; i<=n; i++)
		{
			sum = x+y;
			x = y;
			y = sum;
			
			printf(" %d", sum);
		}
	}
	
	return 0;
}
