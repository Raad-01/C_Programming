#include <stdio.h>

int main()
{
	int n, x, y, i, a, d, r, sum = 0;
	scanf("%d%d%d", &n, &x, &y);
	
	for(i=1; i<=n; i++)
	{
		a = i;
		d = 0;
		
		while(a!=0)
		{
			r = a%10;
			d = d+r;
			a = a/10;
		}
		
		if(d>=x && d<=y)
		{
			sum = sum + i;
		}
	}
	
	printf("%d\n", sum);
	
    return 0;
}
